version: '3.8'

services:
  mcp-pdf-server:
    build: .
    container_name: mcp-pdf-converter
    volumes:
      - ./pdfs:/app/input
      - ./docs:/app/docs
    environment:
      - OUTPUT_DIR=/app/docs
    stdin_open: true
    tty: true