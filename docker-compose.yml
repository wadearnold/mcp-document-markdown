version: '3.8'

services:
  mcp-pdf-markdown:
    build: .
    container_name: mcp-pdf-markdown-converter
    volumes:
      - ./pdfs:/app/input
      - ./docs:/app/docs
    environment:
      - OUTPUT_DIR=/app/docs
    stdin_open: true
    tty: true