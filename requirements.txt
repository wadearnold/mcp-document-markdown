# Core document processing libraries
pypdf>=3.0.0
pdfplumber>=0.9.0
PyMuPDF>=1.23.0
markitdown[all]>=0.1.0  # Microsoft's document to markdown converter

# Data processing and formatting
pandas>=2.0.0
pillow>=10.0.0
tabulate>=0.9.0
markdown>=3.4.0

# Token counting for LLM optimization
# This is optional but highly recommended for accurate token counts
tiktoken>=0.5.0

# MCP server framework
mcp>=1.0.0

# Note: Structured table output uses JSON format only
# JSON is optimal for LLM processing - no additional dependencies needed